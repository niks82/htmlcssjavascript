IN OPERATOR

SELECT <COLUMN_LIST>
FROM <TABLE_NAME>
WHERE <COLUMN_NAME> IN (V1,V2,V3,_____,VN);

LIKE OPERATOR

SELECT <COLUMN_LIST>
FROM <TABLE_NAME>
WHERE <COLUMN_NAME> LIKE 'PATTERN' {[ESCAPE 'escape_char']};

BETWEEN OPERATOR

SELECT <COLUMN_NAME>
FROM <TABLE_NAME>
WHERE <COLUMN_NAME> VALUE1 AND VALUE2;

IS OPERATOR 

SELECT <COLUMN_NAME>
FROM <TABLE_NAME>
WHERE <COLUMN_NAME> IS NULL;

ORDER BY CLAUSE

SELECT <COLUMN_NAME>
FROM <TABLE_NAME>
WHERE <CONDITION/S>
ORDER BY <COLUMN_NAME> ASC/DESC;

FUNCTIONS

FUNCTION function_name(arg1,arg2,____,argn)
{
STATEMENTS;
}

REPLACE()

REPLACE('STRING','STRING_TO_BE_REPLACED','REPLACEMENT_STRING');

SUBSTR()

SUBSTR(STRING,START_POSITION,LENGTH);

INSTR()

INSTR(STRING,SUBSTR,START_POSITION,NTH_OCCURENCE);

LTRIM(STRING,TRIM_STRING);

RTRIM(STRING,TRIM_STRING);

TRIM(LEADING/TRAILING/BOTH TRIM_CHARACTER FROM STRING);

NVL(ARG1,ARG2);

NVL2(ARG1,ARG2,ARG3);

GROUP BY CLAUSE

SELECT <COLUMN_LIST>,<AGGREGATE_FUNCTIONS>
FROM <TABLE_NAME>
WHERE <CONDITION/S>
GROUP BY <COLUMN_LIST>
HAVING <CONDITION_BASED_ON_GROUP BY CLAUSE>

INNER JOIN

ORACLE SYNTAX
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1,T2
WHERE T.COMMON_COLUMN=T2.COMMON_COLUMN;

ANSI SYNTAX
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1 JOIN T2
ON T1.COMMON_COLUMN=T2.COMMON_COLUMN

LEFT OUTER JOIN

ORACLE SYNTAX
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1,T2
WHERE T1.COMMON_COLUMN=T2.COMMON_COLUMN(+);

ANSI SYNTAX
SELECT T1.COULUMN_NAME,T2.COLUMN_NAME
FROM T1 LEFT OUTER JOIN T2
ON T.COMMON_COLUMN=T2.COMMON_COLUMN;

RIGHT OUTER JOIN

ORACLE SYNTAX
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1,T2
WHERE T1.COMMON_COLUMN(+)=T2.COMMON_COLUMN;

ANSI SYNTAX
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1 RIGHT OUTER JOIN T2
ON T1.COMMON_COLUMN=T2.COMMON_COLUMN;

FULL OUTER JOIN

ORACLE SYNTAX
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1,T2
WHERE T1.COMMON_COLUMN=T2.COMMON_COLUMN(+);
UNION
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1,T2
WHERE T1.COMMON_COLUMN(+)=T2.COMMON_COLUMN;

ANSI SYNTAX
SELECT T1.COLUMN_NAME,T2.COLUMN_NAME
FROM T1 FULL OUTER JOIN T2
ON T1.COMMON_COLUMN=T2.COMMON_COLUMN;